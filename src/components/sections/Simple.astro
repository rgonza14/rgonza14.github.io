---
type FM = {
    id: string;
    title: string;
};

const { frontmatter, Content } = Astro.props as {
    frontmatter: FM;
    Content?: any;
};

const { id, title } = frontmatter;
---

<section id={id} class='flex flex-col gap-6 scroll-mt-20'>
    <h2 class='tracking-wide text-primary'>{title}</h2>

    {
        Content && (
            <div class='prose prose-invert max-w-none text-white/90'>
                <Content />
            </div>
        )
    }
</section>
